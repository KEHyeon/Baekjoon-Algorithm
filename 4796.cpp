#include <iostream>
using namespace std;
int a, b, c, num;
int main() {
    cin >> a >> b >> c;
    while(a != 0 && b != 0 && c != 0) {
        cout << "Case " << num << ""
        cin >> a >> b >> c;

    }
}