#include <bits/stdc++.h>
using namespace std;
int tc, n, goal, coin[20];

int main() {
    int tc; cin >> tc;
    while(tc--) {
        cin >> n;
        for(int i = 0; i < n; i++) cin >> coin[i];
        cin >> goal;
    }
}